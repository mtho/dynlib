#include <stdio.h>

double calcCircleArea(double r){
//	printf("Ergebnis: %lf\n", r);
	return r*r*3.14;
}

double calcRectArea(double a, double b) {
//	printf("Ergebnis wird berechnet fuer %i und %i\n", (int) a, (int) b);
	return a*b;
}
